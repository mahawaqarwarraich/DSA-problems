#include<iostream>
using namespace std;





bool isMatch(string text, string doublePattern) {
    
}


int main() {
    string pattern = "water";
    string text = "terwaabcd";
    string doublePattern = pattern + pattern; // lets consider it a text now

    isMatch(text, doublePattern);
    return 0;

}